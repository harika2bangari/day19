#include <stdio.h>

main() {
    int n, i;
i=1;
    printf("Enter the value of n: ");
    scanf("%d", &n);

    printf("The first %d even numbers are: ", n);
    while ( i <= n) {
        printf("%d ", 2*i);
        i++;
    }

    
}
